⚠️ REMINDER (DO THIS FIRST)

Block kernel RST or Linux will nuke your connection:

sudo iptables -A OUTPUT -p tcp --tcp-flags RST RST -j DROP


Remove later:

sudo iptables -D OUTPUT -p tcp --tcp-flags RST RST -j DROP
