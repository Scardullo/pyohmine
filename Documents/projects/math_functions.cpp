#include <iostream>
#include <cmath>

int main() {

    double n = 3.14;
    double x = 3;
    double y = 4;
    double z;

    //z = std::max(x, y);
    //z = std::min(x, y);
    //z = pow(2, 3);        // <- 2 to the power of 3
    //z = sqrt(9);
    //z = abs(-3);          // absolute value
    //z = round(n);
    //z = ceil(n);          // round up
    //z = floor(n);         // round down
    
    std::cout << z;

    return 0;
}